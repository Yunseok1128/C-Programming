#include <stdio.h>

int main(void)
{
	int x, y;


	if (x > y)
	{
		printf("x의 값은 y의 값보다 크다. \n");
	}
	else (x < y)
	{
		printf("x의 값은 y의 값보다 작다. \n");
	}
	else if (x = y)
	{
		printf("x의 값은 y와 값과 같다. \n");
	}
	
	return 0;
}